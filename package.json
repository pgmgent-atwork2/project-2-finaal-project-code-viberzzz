{
  "name": "project-2-finaal-project",
  "version": "1.0.0",
  "description": "Full-stack Supabase project with auth and React frontend",
  "private": true,
  "scripts": {
    "dev": "cd full-stack && npm start",
    "start": "cd full-stack && npm start",
    "build": "cd full-stack && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14"
  }
}
