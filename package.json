{
  "name": "project-2-finaal-project",
  "version": "1.0.0",
  "description": "Full-stack Supabase project with auth and React frontend",
  "private": true,
  "workspaces": [
    "services/frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "start:frontend": "npm --workspace=services/frontend start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14"
  }
}
